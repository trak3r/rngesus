<% content_for :title, "Rolls" %>

<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Rolls</h1>
    <%= link_to "New roll", new_roll_path, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white block font-medium" %>
  </div>

  <%= render 'index' %>
</div>
