<% content_for :title, "Editing roll" %>

<div class="max-w-4xl mx-auto py-10 px-4">
  <h1 class="font-bold text-4xl font-typewriter text-base-content mb-6">Editing roll</h1>

  <%= render "form", roll: @roll %>

  <%= link_to "Back", @roll, class: "w-full sm:w-auto text-center mt-4 sm:ml-2 rounded-md px-3.5 py-2.5 text-base-content/50 hover:text-base-content inline-block font-handwriting text-xl transition-colors" %>
</div>
