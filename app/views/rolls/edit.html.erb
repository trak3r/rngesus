<% content_for :title, "Editing roll" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl font-typewriter text-base-content mb-6">Editing roll</h1>

  <%= render "form", roll: @roll %>

  <%= link_to "Back", randomizer_path(@roll.randomizer), class: "w-full sm:w-auto text-center mt-4 sm:ml-2 rounded-md px-3.5 py-2.5 text-base-content/50 hover:text-base-content inline-block font-handwriting text-xl transition-colors" %>
</div>
