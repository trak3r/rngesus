<% content_for :title, "New result" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl font-typewriter text-pencil mb-6">New result</h1>

  <%= render "form", result: @result %>

  <%= link_to "Back to roll", roll_path(@result.roll), class: "w-full sm:w-auto text-center mt-4 sm:ml-2 rounded-md px-3.5 py-2.5 text-pencil/50 hover:text-pencil inline-block font-handwriting text-xl transition-colors" %>
</div>
