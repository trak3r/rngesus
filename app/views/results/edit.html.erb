<% content_for :title, "Editing result" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing result</h1>

  <%= render "form", result: @result %>

  <%= link_to "Back", roll_path(@result.roll), class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 btn-secondary hover:btn-secondary inline-block font-medium" %>
</div>
