<% content_for :title, "Results" %>

<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Results</h1>
    <%= link_to "New result", new_roll_result_path(@roll), class: "rounded-md px-3.5 py-2.5 btn-primary hover:btn-primary0 text-white block font-medium" %>
  </div>

  <%= render 'index', results: @roll.results %>
</div>
