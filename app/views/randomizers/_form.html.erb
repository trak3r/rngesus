<%= form_with(model: randomizer, data: { controller: "reset-form", action: "turbo:submit-end->reset-form#reset" }) do |form| %>
  <% if randomizer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(randomizer.errors.count, "error") %> prohibited this randomizer from being saved:</h2>

      <ul>
        <% randomizer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
