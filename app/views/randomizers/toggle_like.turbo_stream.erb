<turbo-stream action="replace" target="<%= dom_id(@randomizer, :like_button) %>">
  <template>
    <%= render partial: "like_button", locals: { randomizer: @randomizer }, formats: [:html] %>
  </template>
</turbo-stream>
