<% content_for :title, "New results screenshot" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New results screenshot</h1>

  <%= render "form", results_img: @results_img %>

  <%= link_to "Back to roll", roll_path(@roll), class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 btn-secondary hover:btn-secondary inline-block font-medium" %>
</div>
